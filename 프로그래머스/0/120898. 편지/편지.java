class Solution {
    public int solution(String message) {
        int answer = 0;
        final int CHAR_SIZE = 2;
        
        answer = message.length() * CHAR_SIZE;
        
        return answer;
    }
}